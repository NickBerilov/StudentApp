# How to run the app

You need to run both the backend and the frontend in parallel

### Running the backend

`cd backend`
`npm install`
`npm start`

### Running the frontend

`cd frontend`
`npm install`
`npm start`